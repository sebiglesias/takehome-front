export type Balance = {
    weth: string,
    axie: string,
    slp: string,
    axs: string
}

export type TxsPercentages = {
    weth: number,
    axie: number,
    slp: number,
    axs: number,
    ron: number
}

export type Transaction = {
    actionType: string
}

export type TransactionERC20 = {
    actionType: string
}